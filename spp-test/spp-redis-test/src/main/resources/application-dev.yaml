spp:
  disableCrawler: "51daili"

spring:
  #########################################
  # redis configuration
  #########################################
  redis:
    database: 0
    host: 127.0.0.1
    port: 6379
    timeout: 10s
    lettuce:
      pool:
        max-active: 200
        max-wait: -1ms
        max-idle: 10
        min-idle: 0
